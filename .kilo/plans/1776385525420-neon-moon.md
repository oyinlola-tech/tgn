# Plan: TWETA Styling Updates

## Finalized Requirements
- Dark gold color: #B8860B (DarkGoldenRod)
- GLOBAL NETWORK: two sizes smaller than TWETA

## Changes to Make

### 1. Add Dark Gold Color
Add `dark-gold: '#B8860B'` to the Tailwind colors object (line 65-76).

### 2. Update TWETA Color Throughout
Apply `text-dark-gold` class to all TWETA text instances:
- Line 97 (nav): Add `text-dark-gold`
- Line 131 (main h1): Change `text-primary` to `text-dark-gold`
- Line 330 (footer): Add `text-dark-gold`

### 3. Restructure Line 131 (Main Headline)
Current:
```html
<h1 class="font-headline text-3xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-bold tracking-[0.12em] text-primary uppercase leading-tight sm:leading-[1.1]">
  TWETA GLOBAL NETWORK
</h1>
```

Replace with:
```html
<div>
  <h1 class="font-headline text-3xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-bold tracking-[0.12em] text-dark-gold uppercase leading-tight sm:leading-[1.1]">
    TWETA
  </h1>
  <p class="font-headline text-lg sm:text-xl md:text-2xl lg:text-3xl xl:text-4xl font-bold tracking-[0.12em] text-primary uppercase leading-tight sm:leading-[1.1] mt-2">
    GLOBAL NETWORK
  </p>
</div>
```

## Files to Modify
- `index.html` - Lines 65-76 (Tailwind config), 97, 131, 330